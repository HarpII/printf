#include <stdio.h>
#include "main.h"

/**
 * _printf - produces output according to a format
 * @format: string with indentifiers
 *
 * Return: number of characters printed
 */
int _print(const char *format, ...);
{
	va_list ap;


	va_start(ap, format);



	va_end(ap);
}
